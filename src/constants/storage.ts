/** localStorage / sessionStorage keys. Namespaced to avoid collisions. */
export const STORAGE_KEYS = {
  theme: "renuterra.theme",
  sidebarCollapsed: "renuterra.sidebar-collapsed",
  commentAuthor: "renuterra.comment-author",
  pinnedApps: "renuterra.pinned-apps",
} as const;
